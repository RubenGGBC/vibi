fastapi>=0.115
python-multipart>=0.0.9
uvicorn[standard]>=0.30
pydantic-settings>=2.4
bcrypt>=4.2
PyJWT>=2.9
groq>=0.11
edge-tts>=7.0
anthropic>=0.40
claude-agent-sdk>=0.1
python-telegram-bot>=21.6
pypdf>=5.0
python-docx>=1.1
cryptography>=43.0
httpx>=0.27
# Motor de chat Antigravity: la CLI `agy` comprueba que hay un terminal de
# verdad antes de arrancar, así que hay que abrirla en un pseudoterminal. En
# Windows eso es ConPTY (pywinpty); fuera, ptyprocess. Sin esto el motor no
# arranca y responde Claude.
pywinpty>=3.0; sys_platform == "win32"
ptyprocess>=0.7; sys_platform != "win32"
